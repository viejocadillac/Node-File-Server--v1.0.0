eebhtml
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title Element - File Server
    link(href='http://192.168.1.240:3000/styles/bootstrap.min.css' rel='stylesheet')
    script(src= 'http://192.168.1.240:3000/scripts/main.js' type= 'application/javascript')
  
  body
    div.container
      h3 Bienvenido a Element FileServer
      h6 Los siguientes archivos estan disponibles para su descarga:
      
      table.table.table-striped
        thead
          tr
            th Nombre
            th Ruta

        tbody
          - vacio = dt != undefined;
          if vacio
            each f in dt
              - archivo = f.type == "file";
              - directorio = f.type == "directory";
               
              if archivo
                tr
                  td
                    a(href='http://192.168.1.240:3000/' + f.path) #{f.name}
                  td
                    p #{f.path}  
              if directorio
                tr
                  td
                    a(href='http://192.168.1.240:3000/' + f.path) #{f.name}
                  td
                    p #{f.path}
                    
